package edu.uci.Inf122.TileMatchingMania.GUI.Drawable;

import java.awt.*;

/*
 * Each Drawable type will have to implement a draw method
 * so that a tile's visual representation can be rendered in a GUI.
 */
public interface Drawable {
    Image draw();
}
